package Observer.antiPattern;

public class DisplayCelular {
    public void update(double temperatura) {
        System.out.println("Display Celular: Temperatura atual é " + temperatura + "°C");
    }
}
